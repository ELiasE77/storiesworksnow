# ?????????????????????????????????????
# APPLICATION PROPERTIES
# ?????????????????????????????????????

# App name & static resources
spring.application.name=Journal_site
spring.web.resources.static-locations=classpath:/static/

# MySQL datasource
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/login?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Elias337!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate / JPA
# Change from validate ? update so Hibernate will add missing columns
spring.jpa.hibernate.ddl-auto=update

# (Optional) Show the SQL being executed
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
